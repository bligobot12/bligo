# 2026-03-07

## Bligo overnight + morning progress (durable)

- Published GitHub repo: `https://github.com/bligobot12/bligo`.
- Cloudflare Pages deployment succeeded initially after fixing missing Next.js deps and version conflicts.
- Versioning updates completed:
  - `v1.003.001` tagged/pushed.
  - Later bumped to `v1.003.002` in repo.

## Major shipped feature milestones

- Landed multi-page marketing shell (home/about/contact) with shared layout + styling.
- Shipped MVP app feature set (client-state based):
  - auth flow (`/login`, `/signup`),
  - dashboard (`/app`),
  - profile editing + avatar upload (`/app/profile`),
  - posts feed/create (`/app/posts`),
  - messaging thread/send (`/app/messages`),
  - bot connect toggle (`/app/bot`).
- Added Supabase production scaffold:
  - clients (`lib/supabase/client.js`, `lib/supabase/server.js`),
  - SQL schema + RLS (`supabase/schema.sql`),
  - setup docs (`PROD_BACKEND_SETUP.md`),
  - env template (`.env.example`),
  - backend status page (`/app/backend-status`).

## User preferences / working agreement

- User explicitly requested autonomous “build mode overnight”:
  - keep shipping without waiting,
  - skip blockers, isolate, and continue,
  - only ping when truly needed.
- User is comfortable with browser-relay assisted actions for GitHub/Cloudflare/Supabase.

## Current blocking issue (important)

- Live `bligo.pages.dev` currently failing with Node compatibility error.
- Root cause confirmed in Cloudflare deploy logs:
  - contradictory flags: `nodejs_compat_populate_process_env` vs `nodejs_compat_do_not_populate_process_env`
  - invalid flag: `node`
- Required state for Pages runtime flags:
  - keep only `nodejs_compat` (remove conflicting/invalid extras), then redeploy.

## Notes

- Supabase project was created and env keys were added locally (`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`).
- Supabase schema execution in dashboard eventually succeeded after adjusting policy syntax and reruns.
